{% extends "STAppBundle::layout.html.twig" %}

{% block body %}

	<div class="container">
		<div class="profile">

		  <h2>Bienvenue sur votre profil "{{ app.user.username }}"</h2>
		  
		  <p>Votre nom d'utilisateur : {{ app.user.username }}</p>
		  <p>Votre prénom : {{ app.user.firstname }}</p>
		  <p>Votre nom de famille : {{ app.user.lastname }}</p>
		  <p>Votre adresse email : {{ app.user.email }}</p>
		  <p>Votre image de profil : <img src="{{ asset(app.user.picture.webPath) }}" alt="{{ app.user.picture.alt }}" style="width: 10%" ></p>

          <div class="row">
			  <div class="col-sm-2">
			  		<img src="{{ asset(app.user.picture.webPath) }}" alt="{{ app.user.picture.alt }}" style="width: 40%">
			  </div>
			  <div class"col-sm-10">
				  	<h4>{{ app.user.firstname }} {{ app.user.lastname }}</h4>
				  	<hr>
				  	<p>Nom d'utilisateur : {{ app.user.username }}</p>
				  	<p>Adresse email : {{ app.user.email }}</p>
			  </div>
		  </div>
		</div>
	</div>

{% endblock %}