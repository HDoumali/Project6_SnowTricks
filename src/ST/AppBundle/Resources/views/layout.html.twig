<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %}Snowtricks{% endblock %}</title>
    <!--pour le css perso, pour charger les fichier du repertoire css, nous pouvons utiliser le joker '*' => bundles/ocplatform/css/* -->
    {% block stylesheets %}
      {# On charge le CSS de bootstrap depuis le site directement #}
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
      <link rel="stylesheet" href="{{ asset('css/main.css') }}" type="text/css" />
      <link rel="stylesheet" href="{{ asset('css/font-awesome/css/font-awesome.min.css') }}" type="text/css" />
      <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.1.0/css/all.css" integrity="sha384-lKuwvrZot6UHsBSfcMvOkWwlCMgc0TaWr+30HWe3a4ltaBwTZhyTEggF5tJv8tbt" crossorigin="anonymous">
      <link href="https://fonts.googleapis.com/css?family=Lobster|Roboto" rel="stylesheet">
    {% endblock %}
    
  </head>

  <body>
      <nav class="navbar navbar-default hidden-xs">
          <div class="container">
              <a href="{{ path('st_app_home') }}" class="navbar-brand">
                  <i class="fas fa-snowflake"></i>
                  <span>SnowTricks</span>
                  <i class="fas fa-snowflake"></i>
              </a>
              <ul class="nav navbar-nav navbar-right">
                  <li>
                      <a href="{{ path('st_app_home') }}"><span class="glyphicon glyphicon-home" aria-hidden="true"></span></a>
                  </li>
                  <li>
                      <a href="#">TRICKS</a>
                  </li>
                  {% if is_granted('ROLE_USER') %}
                    <li>
                        <a href="{{ path('profile') }}"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> {{ app.user.firstname|upper }} {{ app.user.lastname|upper }}</a>
                    </li>
                  {% endif %}
                  {% if not is_granted('ROLE_USER') %}
                  <li>
                      <a href="{{ path('login') }}"><span class="glyphicon glyphicon-log-in" aria-hidden="true"></span> CONNEXION</a>
                  </li>
                  {% endif %}
                  {% if is_granted('ROLE_USER') %}
                    <li>
                        <a href="{{ path('logout') }}"><span class="glyphicon glyphicon-off" aria-hidden="true"></span> DECONNEXION</a>
                    </li>
                  {% endif %}
                  {% if not is_granted('ROLE_USER') %}
                  <li>
                      <a href="{{ path('register') }}"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> INSCRIPTION</a>
                  </li>
                  {% endif %}
              </ul> 
          </div>
      </nav>

      {% block body %}
      {% endblock %}

    <footer>
      <!--<div class="container">-->
                <div class="row">
                    <div class="col col-md-12">
                        <h3>Suivez-nous sur les réseaux sociaux :</h3>
                        <ul class="list-inline">
                            <li>
                                <a href="https://twitter.com/?lang=fr" target="blank" class="btn-social btn-outline"><i class="fab fa-2x fa-twitter"></i></a>
                            </li>
                            <li>
                                <a href="https://www.facebook.com/" target="blank" class="btn-social btn-outline"><i class="fab fa-2x fa-facebook"></i></a>
                            </li>
                            <li>
                                <a href="https://www.instagram.com/" target="blank" class="btn-social btn-outline"><i class="fab fa-2x fa-instagram"></i></a>
                            </li>
                        </ul><br />
                </div>
      <!--</div>-->
      <p>Copyright © {{ 'now'|date('Y') }} - Réalisé par <a class="copyright" href="https://github.com/HDoumali/" target="blank" class="btn-social btn-outline">Hassan DOUMALI</a></p>
    </footer>
    
       <nav class="navbar hidden-sm hidden-md hidden-lg navbarmenuxs">
            <div class="container">
                <ul class="nav navbar-nav navbar-center" id="navbarxs">
                  <div class="row">
                    <div class="col-xs-4">
                        <li>
                            <a href="{{ path('st_app_home') }}"><i class="fas fa-2x fa-home"></i></a>
                        </li>
                    </div>
                    <div class="col-xs-4">
                        <li>
                            <a href="#"><i class="fas fa-2x fa-bookmark"></i></a>
                        </li>
                    </div>
                    <div class="col-xs-4">
                        {% if not is_granted('ROLE_USER') %}
                        <li>
                            <a href="{{ path('login') }}"><i class="fas fa-2x fa-sign-in-alt"></i></a>
                        </li>
                        {% endif %}
                    </div>
                    <div class="col-xs-4">
                        {% if is_granted('ROLE_USER') %}
                          <li>
                              <a href="{{ path('logout') }}"><i class="fas fa-2x fa-sign-out-alt"></i></a>
                          </li>
                        {% endif %}
                    </div>
                </ul> 
            </div>
        </nav>
  
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>

  {% block javascripts %}
    {# Ajoutez ces lignes JavaScript si vous comptez vous servir des fonctionnalités du bootstrap Twitter #}
    
  {% endblock %}

  
  </body>
</html>
